{
  "name": "bricknellbarlow-2024",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "go run assets/build.go",
    "prod": "mv .env .env-local && mv .env-prod .env && go build -o ./index && mv .env .env-prod && mv .env-local .env"
  },
  "dependencies": {
    "sass": "^1.71.1",
    "uglify-js": "^3.17.4"
  }
}
